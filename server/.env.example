# Create a MongoDB Atlas cluster, then copy its connection string here.
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/jay-portfolio?retryWrites=true&w=majority
PORT=5000
CLIENT_ORIGIN=http://localhost:5173
# Use this only if your DNS blocks MongoDB Atlas SRV lookups.
MONGODB_DNS_SERVERS=1.1.1.1,8.8.8.8

# Gmail SMTP: create an App Password in your Google Account, never use your normal password.
SMTP_USER=jcomendador120@gmail.com
SMTP_PASS=your_16_character_gmail_app_password
CONTACT_RECIPIENT=jcomendador120@gmail.com
