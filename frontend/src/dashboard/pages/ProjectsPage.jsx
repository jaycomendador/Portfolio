import { ExternalLink, Pencil, Plus } from 'lucide-react';

export default function ProjectsPage({ projects, onEdit, onOpenAdd, onOpenEdit }) {
  return <section className="dashboard-panel projects-page"><div className="panel-heading projects-heading"><div><p className="dashboard-kicker">WORKSPACE</p><h2>Projects</h2><span>Manage the work displayed in your portfolio.</span></div><div className="projects-actions"><button type="button" className="project-secondary-button" onClick={onEdit}><Pencil size={15} /> Edit portfolio</button><button type="button" className="project-primary-button" onClick={onOpenAdd}><Plus size={16} /> Add new project</button></div></div><div className="projects-panel-grid">{projects.length ? projects.map((item) => <ProjectCard key={item.id || item.name} project={item} onEdit={() => onOpenEdit(item)} />) : <div className="empty-state"><strong>No projects match your search.</strong><span>Try another search or add a new project.</span></div>}</div></section>;
}

function ProjectCard({ project, onEdit }) { return <article className="project-card"><div className="project-card-icon">{project.name.slice(0, 1)}</div><div className="project-card-content"><div className="project-card-topline"><em className={project.status.toLowerCase().replace(' ', '-')}>{project.status}</em><span>{project.url && <a href={project.url} target="_blank" rel="noreferrer" aria-label={`Open ${project.name}`}><ExternalLink size={15} /></a>}<button type="button" onClick={onEdit} aria-label={`Edit ${project.name}`}><Pencil size={14} /></button></span></div><h3>{project.name}</h3><p>{project.detail}</p><small>{project.technologies || 'Technologies not specified'}</small></div></article>; }
